/**
 * Database Schema Exports
 * Central export point for all Drizzle schemas
 *
 * NOTE:
 * The actual table definitions now live under `src/database/schemas`.
 * This file simply re-exports that central schema index so Drizzle
 * can import everything from one place if needed.
 */

export * from '../../schemas';
