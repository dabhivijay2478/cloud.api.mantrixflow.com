export enum SubscriptionPlan {
  FREE = 'free',
  PRO = 'pro',
  SCALE = 'scale',
  ENTERPRISE = 'enterprise',
}

export enum SubscriptionStatus {
  ACTIVE = 'active',
  ON_HOLD = 'on_hold',
  FAILED = 'failed',
  CANCELED = 'canceled',
  TRIALING = 'trialing',
}
