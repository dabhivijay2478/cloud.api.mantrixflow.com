meta {
  name: MantrixFlow PostgreSQL Connector API
}

auth {
  mode: none
}

vars:pre-request {
  baseUrl: http://localhost:8000
  baseUrl1: https://api.mantrixflow.com
  connectionId: 
  syncJobId: 
  tableName: pg_user
}

docs {
  REST API for managing PostgreSQL connections, schema discovery, query execution, and data synchronization.
}
