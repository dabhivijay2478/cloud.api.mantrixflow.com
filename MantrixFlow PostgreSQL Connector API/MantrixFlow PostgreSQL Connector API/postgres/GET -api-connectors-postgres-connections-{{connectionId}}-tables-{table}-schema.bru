meta {
  name: GET /api/connectors/postgres/connections/{{connectionId}}/tables/{table}/schema
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/api/connectors/postgres/connections/{{connectionId}}/tables/{table}/schema
  body: none
  auth: inherit
}

params:query {
  schema: 
}

settings {
  encodeUrl: true
}
