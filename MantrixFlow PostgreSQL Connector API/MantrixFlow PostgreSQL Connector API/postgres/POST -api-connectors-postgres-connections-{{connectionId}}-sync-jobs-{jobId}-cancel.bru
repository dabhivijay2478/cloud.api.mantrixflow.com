meta {
  name: POST /api/connectors/postgres/connections/{{connectionId}}/sync-jobs/{jobId}/cancel
  type: http
  seq: 17
}

post {
  url: {{baseUrl}}/api/connectors/postgres/connections/{{connectionId}}/sync-jobs/{jobId}/cancel
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}
